import { View, Text } from "react-native";
import React from "react";

const AddNew = () => {
  return (
    <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
      <Text>Halaman Tambah Menu baru</Text>
    </View>
  );
};

export default AddNew;
